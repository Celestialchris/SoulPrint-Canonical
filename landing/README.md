# Landing surface

This directory contains the current static landing page for `soulprint.dev`.

It remains the production landing surface while the SvelteKit marketing site under `site/` develops as the future replacement track.

Do not treat this directory as the Flask app or the canonical ledger runtime.
