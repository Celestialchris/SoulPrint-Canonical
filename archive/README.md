# Archive Notice

Content under `archive/` is historical and non-authoritative.

- Do not use these files as active product doctrine.
- Current product truth lives in active root/docs surfaces (for example: `README.md`, `ROADMAP.md`, `docs/product/*`, `docs/specs/*`).
- Archive files are retained for historical context and traceability only.
