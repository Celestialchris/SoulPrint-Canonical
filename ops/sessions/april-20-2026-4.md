## April 20, 2026 (evening) — P7 Phase 1 shipped, RTK active, branch rescue, Codex fixes

**Branch:** main (PR #132 open on feat/p7-phase1-cli-dispatch with Codex fixes pushed)
**Version:** v0.7.0-alpha.3 (unchanged)

**What shipped:**
- PR #131 — Two learned patterns from the CodeQL session merged.
- PR #132 — P7 Phase 1: CLI subcommand dispatch. `src/cli.py` introduces argparse dispatch for `serve`, `info`, and `mcp-config`. Bare `soulprint` still starts the server (backwards compatible). `pyproject.toml` console script target changed from `src.main:main` to `src.cli:main`. `info` uses raw sqlite3, skipping Flask app init. `mcp-config` prints a ready-to-paste `.mcp.json` block. 880 tests pass. Codex flagged two issues post-open (P1: honor SOULPRINT_DB; P2: use installed entry point instead of bare python); both fixed in a follow-up commit on the same branch.

**Observations:**
- RTK is active in Claude Code sessions. Visible as `rtk git`, `rtk pytest` in command previews. Compressed outputs like "Pytest: 9 passed" confirm the PreToolUse hook is rewriting. Caveat: `rtk pytest` with no args returned "No tests collected" while `rtk pytest tests/` worked and bare `python -m pytest` returned 880 passing. Likely an rtk bug on no-args invocation. Worth filing upstream at github.com/rtk-ai/rtk.

**Incidents:**
- Claude Code committed P7 Phase 1 to `main` instead of `feat/p7-phase1-cli-dispatch` despite explicitly running `git checkout -b` at the start of the session. Rescued via cherry-pick to the feature branch, push, PR #132 opened cleanly. Root cause not traced; possibilities include an intermediate `checkout main` dropping context, or an rtk wrapper interaction.
- Local `main` over-rewound after rescue. First `git reset --hard origin/main` was correct (synced to origin). A second `git reset --hard HEAD^` overshot by one commit (dropped PR #131's merge). Recovered via `git pull` which fast-forwarded back to `origin/main`.

**Decisions:**
- `git reset --hard origin/<branch>` is the safe sync pattern. Never use `HEAD^` when there is any chance of running the command twice. Promoted to ops/learned/.
- After `git checkout -b`, always verify with `git branch --show-current` before any `git commit`. Promoted to ops/learned/.

**Next:**
- Merge PR #132 once Codex fixes pass review.
- Queued: Prompt 3c (per-id summary/distill delete routes), P3 drop folder, companion layer prompts CP1/CP4/CP7/CP8.
