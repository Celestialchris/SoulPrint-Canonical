# Session Log, 2026-04-27 (2)

**Branch:** `docs/readme-security-trust-rewrite`
**Scope:** README and SECURITY trust-language rewrite (Template H execution).
**Files edited:** `README.md`, `SECURITY.md`, this session log.
**Files NOT edited:** `LICENSE`, `NOTICE`, `pyproject.toml`, `CHANGELOG.md`, `ROADMAP.md`, `src/`, `docs/`, `context/`, `.claude/`, tests, CSS, release workflows.

## Goal

Rewrite the public face of SoulPrint (README + security policy) so it presents as a serious, truthful, trust-centered local-first archive. Replace claims that did not match shipped reality, give the architecture a real diagram and stack table, expand the security policy from a four-section stub into a real threat model, and add a compact "Interface roadmap" checkpoint trail that links to the frontend evolution doctrine without rebranding the runtime.

## Verified facts at the start of the session

- **Release assets at v0.6.0 are real.** GitHub release lists three assets uploaded 2026-04-17: `SoulPrint-0.6.0-linux.tar.gz`, `SoulPrint-0.6.0-macos.zip`, `SoulPrint-0.6.0-windows.zip`.
- **No installer was ever published.** The previous README claimed `SoulPrint-Setup.exe`. That file is not in the v0.6.0 release. Build script paths exist in `scripts/` but no Inno Setup output was ever uploaded.
- **Source tree is at v0.7.0a2** (per `pyproject.toml`). Significant features have shipped between v0.6.0 and HEAD: attachment-aware exports, archive health observability, ImportRun history, Quiet Archive v3 visual direction, Claude Code importer, MCP server, etc. The packaged builds are stale relative to source.
- **Frontend evolution doctrine** lives at `docs/specs/frontend-evolution-doctrine.md`. It is a doctrine, not a shipped change. Current runtime remains Flask, Jinja2, SQLite, SQLAlchemy, HTML/CSS.
- **Five providers** supported: ChatGPT, Claude, Claude Code, Gemini, Grok.

## How the install language was resolved

The Stop Conditions explicitly call out the trap: do not delete accurate release information just to make the README sound humbler. Path I took:

- Lead with source install (the prompt's recommended language) because it is the truthful path to the latest behavior.
- Keep a secondary paragraph pointing at v0.6.0 GitHub Releases, with an explicit note that the current source tree ships features the packages predate.
- Drop the `SoulPrint-Setup.exe` claim entirely; only the zips exist, so only the zips get advertised.

This keeps the verified information (three real packaged builds at v0.6.0) without lying about an installer or pretending the last release matches the current code.

## README, structural changes

- Hero rewritten to the prompt's definition: "SoulPrint is a local-first memory archive for AI conversations. It imports chats from ChatGPT, Claude, Claude Code, Gemini, and Grok into a private SQLite ledger, then lets you browse, search, continue, and export the work with provenance intact."
- "Why this exists" no longer claims Chris uses Grok every day. Replaced with the prompt's neutral "I use AI tools every day" framing, preserving the March 2026 silo-launch contrast.
- New "What SoulPrint does" section using the seven-beat rhythm from the prompt (bring home, preserve, search, ask, continue, export, custody).
- New "Architecture" section with a real ASCII pipeline diagram (provider exports → importers → SQLite ledger → FTS / intelligence / export) and a compact stack table.
- New "Data and trust" section consolidating the trust language and listing what the archive does and does not contact.
- New "Exports" section calling out attachment-aware bundles, the `<stem>.assets/` sibling pattern, and the Obsidian raw-inbox bridge.
- Optional Ollama setup moved below the main install flow so it does not dominate first-read attention.
- Providers table preserved (was already accurate after PR #145).
- MCP section preserved.
- New "Interface roadmap" section summarizing the four-checkpoint path from `docs/specs/frontend-evolution-doctrine.md` and linking to the full doctrine. The summary is explicit that the current stack is intentional and that SQLite remains canonical.
- Support and License sections preserved with light language polish; license section reduced to a factual one-liner.

## SECURITY.md, structural changes

The previous file was a 16-line stub. The rewrite expands it into a full security policy without inventing any contact details (the existing GitHub Security Advisories link is preserved as the only reporting channel).

Sections added:

- **Supported versions** matrix (0.7.x active, 0.6.0 best-effort, older unsupported).
- **Security model** with explicit in-scope and out-of-scope lists.
- **Network behavior**, naming the only two outbound paths (configured LLM provider and stdio MCP).
- **LLM provider behavior**, naming the three supported providers and the loopback caveat for `SOULPRINT_LLM_BASE_URL`.
- **Import parser attack surface**, listing all five formats and the typed-error hardening posture.
- **Attachments and file custody**, calling out path-traversal rejection and per-conversation isolation in multi-select zip export.
- **SQLite and archive protection**, with practical guidance on encrypted volumes and the `soulprint verify` audit path.
- **MCP and local server exposure**, with explicit don'ts for binding, proxying, and container exposure.
- **Best practices** and **Out of scope** lists.

No security contact was invented. Reporting still routes through GitHub Security Advisories only.

## Stop Conditions, how each was handled

- **README documents verified release assets.** v0.6.0 binaries do exist; the rewrite preserves them as a secondary path with a staleness note rather than deleting them, while removing the never-published installer claim.
- **No SECURITY.md.** A SECURITY.md exists; the rewrite expands it.
- **License changes.** None requested or made; `LICENSE` and `pyproject.toml` license metadata untouched.
- **Files outside scope lock.** Edits are limited to `README.md`, `SECURITY.md`, and this session log.
- **Packaged builds without verification.** The README only references the verified v0.6.0 zips and is explicit that they are stale relative to source.
- **Frontend doctrine described as current behavior.** The Interface roadmap section is explicitly forward-looking; current stack is named (Flask, Jinja2, SQLite) and the doctrine is linked, not copied wholesale.
- **SECURITY contact invention.** Existing GitHub Security Advisories link preserved as the sole channel.

## Verification

This change is docs-only. Per the prompt and `CONTRIBUTING.md`, no test run is required for documentation changes. The full pytest suite was not run.

`git diff -- README.md SECURITY.md` was used to inspect the rewrite before commit.

## Followups (not in this branch)

- The MCP loopback caveat in SECURITY.md tracks the same P1 followup already in `ROADMAP.md` § Parked: "Resolve deferred P1: loopback restriction on `SOULPRINT_LLM_BASE_URL`." When that lands, the caveat paragraph in SECURITY.md should be deleted.
- When v0.7.x ships packaged builds, the install section in README should swap the v0.6.0 secondary paragraph for current-version language.
- When the first JSON endpoint family lands (per the Interface roadmap), the README's stack table should add an "API surface" row.
