# Session - 2026-04-27 (6) - Product Doc Truth Cleanup

## Branch

`docs/product-doc-cleanup`

## Files changed

- `docs/product/brand.md`
- `docs/product/positioning.md`
- `docs/product/landscape.md`
- `ops/sessions/april-27-2026-6.md` (this file)

## What changed

### `docs/product/brand.md`

Deleted the `## Warm Nav Labels` section and its 10-row table that
duplicated live sidebar labels. Replaced with a `## Sidebar Labels`
section that points readers at `src/app/templates/base.html` as the
authority for sidebar labels.

The Provider Lanes color-token table was left untouched per Verified
Facts. Lanes are defined in `src/app/static/app.css`; no
`--lane-claude-code` token exists and none was added. Claude Code
shares the Claude visual lane in `base.html` via `lane='claude'`.

### `docs/product/positioning.md`

Expanded the stale 3-provider bullet under "What SoulPrint Does Today"
from `ChatGPT, Claude, and Gemini` to the full current list:
`ChatGPT, Claude, Claude Code, Gemini, and Grok`.

### `docs/product/landscape.md`

Two surgical edits.

1. Deleted the `future ChromaDB upgrade path for heavy users` clause
   from the Karpathy structural-cousin paragraph. The sentence now
   ends at `FTS5 search across all imported conversations.` Vector
   DB expansion is not current milestone work per `ROADMAP.md` and
   `DECISIONS.md`, so the clause was removed rather than softened.
2. Corrected the SoulPrint provider list in the MyChatArchive
   competitor paragraph from `ChatGPT, Claude, Gemini, and Grok` to
   `ChatGPT, Claude, Claude Code, Gemini, and Grok`.

## Out of scope

- Existing session logs were not modified.
- No spec, public-doc, source, test, CSS, template, package metadata,
  README, SECURITY, ROADMAP, CHANGELOG, or landing files were touched.
- `src/app/templates/base.html` was read only; not edited.
- No `ops/learned/` file was added. The typography-self-reference
  pattern (point at the live source of truth instead of duplicating
  it) is now applied; it is the same pattern already documented for
  `app.css`, so no new reusable pattern was discovered.

## Verification

- `rtk git diff --check`: clean.
- `grep -nE 'ChatGPT, Claude, Gemini([^,]|$)' docs/product/brand.md docs/product/positioning.md docs/product/landscape.md`: zero matches.
- `grep -nE 'SoulPrint imports ChatGPT, Claude, Gemini' docs/product/landscape.md`: zero matches.
- `grep -nEi 'chromadb|vector db|vector database|vector upgrade|upgrade path' docs/product/brand.md docs/product/positioning.md docs/product/landscape.md`: zero matches.
- `rtk git diff --stat`: only the three product-doc files changed
  (brand.md, positioning.md, landscape.md), 9 insertions and 20
  deletions.
- No pytest run; docs-only branch.

## Followups not in this branch

- Branch 3: landing-page truth rewrite.
- SECURITY/ROADMAP caveat cleanup after the loopback guard branch
  merges, if still needed.
- Contributor-docs sweep for stale provider or sample-data
  references.
