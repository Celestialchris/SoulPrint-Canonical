## April 21, 2026 — CP5.1: MCP soulprint_search sort parity

**Branch:** feat/mcp-search-sort-parity
**Version:** v0.7.0-alpha.3 (patch-level, no bump)

**What shipped:**
CP5.1 landed on feat/mcp-search-sort-parity. Added `sort: str = "relevance"` to `soulprint_search` in `src/mcp_server.py`. Invalid values coerce silently to "relevance" via set-membership check, matching the /federated route convention. `"sort"` is now included in the tool's JSON response so agents can confirm their input was honored. Three new tests in `TestSearch` cover oldest ordering, newest ordering, and invalid-sort fallback. Pure passthrough to `search_fts` -- no downstream changes.

**Decisions:**
- Coerce invalid sort values silently (no error raised), consistent with `/federated` route pattern in `src/app/__init__.py` lines 1243-1244.
- Set literal (`{"relevance", "newest", "oldest"}`) rather than tuple for O(1) membership check.
- Include `"sort"` in response JSON so agents can verify coercion happened (useful when sort="banana" silently becomes "relevance").
- No version bump: patch-level change, no release artifact.

**Next:**
- Candidate queue: landing page refresh, companion layer CP1/CP4/CP7, `.sort-toggle` CSS.

**Test suite:** 914 -> 917, all passing.

**Open threads (no blockers):** PR #136 (fix/cp5-archaeology-followups) still open; this branch is independent.
