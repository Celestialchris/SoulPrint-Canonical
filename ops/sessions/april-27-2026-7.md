# Session Log, 2026-04-27 (7)

**Branch:** `docs/remove-resolved-loopback-caveat`
**Scope:** Remove the resolved keyless-non-loopback caveat from `SECURITY.md` and the corresponding parked followup from `ROADMAP.md` now that PR #178 has merged.
**Files edited:** `SECURITY.md`, `ROADMAP.md`, this session log.
**Files NOT edited:** `src/intelligence/provider.py`, `tests/test_intelligence_provider.py`, existing files under `ops/sessions/`, `ops/learned/`, `README.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `CLAUDE.md`, `AGENTS.md`, `DECISIONS.md`, `pyproject.toml`, `LICENSE`, `NOTICE`, `docs/`, `landing/`, `src/`, `tests/`, `.github/`, `sample_data/`, `scripts/`, `bootstrap/`, `context/`, `.claude/`, CSS, templates, package metadata, dependency files, release files, license validation logic.

## PR #178 merge confirmation

Verified Facts block in the prompt: head ref `fix/llm-base-url-loopback-guard`, merged at `2026-04-27T13:38:30Z`, state `MERGED`, title `Guard keyless LLM base URL to loopback`. After `rtk git pull` on `main`, the merge is reflected locally; the loopback guard code and tests are present on `main`.

## Runtime facts verified from source/tests

Read `src/intelligence/provider.py` (read-only) to confirm the loopback guard is in place:

- `_is_loopback_base_url(url)` parses the URL host with `urllib.parse.urlparse`, accepts the literal `localhost`, otherwise feeds the host into `ipaddress.ip_address(host).is_loopback`. That covers all of `127.0.0.0/8` and IPv6 `::1`. Malformed URLs and unparseable hosts return `False`. No DNS resolution.
- `provider_from_config()` only assigns the dummy `"ollama"` key when `provider_name == "openai"`, `base_url` is non-empty, and `_is_loopback_base_url(base_url)` returns `True`. Any other keyless openai-compatible base URL returns `None` (no provider configured).
- Explicit BYOK to any base URL (loopback or not) is unaffected.

Read `tests/test_intelligence_provider.py` (read-only) to confirm coverage:

- `KeylessLoopbackGuardTest` accepts: `localhost`, `127.0.0.1`, `127.0.0.5`, IPv6 `[::1]`. Rejects: `https://api.example.com/v1`, `http://192.168.1.10:11434/v1`, `[2001:db8::1]`, `"not a url"`. Explicit-key cases accept non-loopback URLs.
- `IsLlmConfiguredTest` adds `test_false_for_keyless_non_loopback_base_url` and `test_true_for_keyless_loopback_base_url`.
- `OllamaFallbackTest` continues to cover the loopback-keyless fallback path.

## SECURITY caveat removed

Deleted the single resolved-caveat paragraph in `SECURITY.md` § LLM provider behavior. The caveat warned that a non-loopback `SOULPRINT_LLM_BASE_URL` could receive prompts under a dummy `api_key` and recommended pointing `BASE_URL` at loopback only until the P1 followup landed. PR #178 enforces that restriction in code, so the warning is now stale.

The surrounding network-behavior, security-model, and BYOK language is unchanged. The remaining valid statement, that the loopback Ollama path requires no API key, stays in the bullet list at the top of the section.

## ROADMAP parked item removed

Deleted the single line `- Resolve deferred P1: loopback restriction on SOULPRINT_LLM_BASE_URL` under § Parked / Phase 11: Soft launch. No other Parked items, no Completed entries, and no surrounding structure were touched. Phase 11 still lists the remaining four launch items (multi-select export, fresh screenshots, landing refresh, Reddit posts).

## Checks run

```
rtk git diff --check
grep -nE 'Known caveat|non-loopback|loopback restriction|SOULPRINT_LLM_BASE_URL' SECURITY.md ROADMAP.md
grep -nP '\x{2014}' ops/sessions/april-27-2026-7.md
rtk git diff --stat
rtk git status --short
```

Expected and observed:

- `git diff --check` clean.
- `grep` against `SECURITY.md` and `ROADMAP.md` returns no `Known caveat` line, no `loopback restriction on SOULPRINT_LLM_BASE_URL` line, and no remaining stale wording. `SOULPRINT_LLM_BASE_URL` may still appear in valid configuration language in other files (none of which are edited here).
- U+2014 sweep on this session log returns zero matches.
- `git diff --stat` shows three files changed: `SECURITY.md`, `ROADMAP.md`, `ops/sessions/april-27-2026-7.md`.

No pytest run for this docs-only branch, per scope.

## Followups not included in this branch

- Contributor-docs sweep (`CONTRIBUTING.md`, `CLAUDE.md`) for stale provider/sample-data references and any lingering doctrine drift.
- Landing-page truth rewrite later, separate branch.
- No new `ops/learned/` entry: this branch reused the existing pattern of small, focused doc-cleanup branches and did not surface a new reusable lesson.

## Existing session logs

Confirmed not modified. Only one new session log was added: `ops/sessions/april-27-2026-7.md`.
