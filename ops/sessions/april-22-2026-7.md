## April 22, 2026 — Phase C visual cleanup: star glyph unify, left rail removal, provider backlinks
**Branch:** chore/phase-c-visual-cleanup
**What:** Unified star glyph across 5 templates to always render `★`, with color carrying the starred state. Removed the left rail aside from base.html and all `.shell .rail*` CSS (rail was 64px of dead UI: duplicated logo, outdated placeholder provider icons (C/G/Ge — only 3 of the 5 providers now covered by /imported), redundant Import shortcut, non-functional settings). Wired Archive Status provider rows in Workspace as links to `/imported?provider=<slug>` with hover treatment matching the existing context-panel interactive patterns.
**Decisions:**
- Row-action CSS class promotion (`.row-action-btn`) deferred to Phase A1 tags MVP, per the CP1 handoff note that called for promotion "when tags/groups ship and introduce a third row action." Phase A1 introduces tag chips, which is that moment.
- Sidebar footer (avatar "C" / "Chris" / "local-first") left intact. Code comment in base.html marks it as intentional OAuth placeholder.
- Workspace top copy rewrite and empty-right-sidebar-below-providers content NOT in scope. Both require design conversation before any code.
- Expanded `tests/test_starring.py` rendering smoke to cover starred state (the unstarred assertion was updated for the glyph change; adding a symmetric starred assertion closes the known coverage gap flagged in the CP1 handoff).
**Next:** Phase A1 tags MVP per `docs/specs/tagging-spec.md` v2. Row-action CSS class promotion lands in A1 as part of introducing tag chips.
