## April 20, 2026 — Claude Code importer landed, Phase 2 drafted

**Branch:** main (post-PR #123 merge)
**Version:** v0.7.0-alpha.3

**What shipped:**
- PR #122 — CP2 archive/hide for imported conversations. `is_archived` column on ImportedConversation, `/imported/archived` page with Unarchive + Delete per row, sidebar "Archived" link under Memory group, `imported_delete_confirm.html` templatized with `return_url` for context-aware back-links. Delete-from-archive flows through the existing 3a confirm page.
- PR #123 — P1 Phase 1: Claude Code importer. 5th provider (`claude_code`) registered. `src/importers/claude_code.py` parses `~/.claude/projects/<project>/<session-uuid>.jsonl`. Tool_use rendered as `[Tool: <n>] <args>` with per-tool smart formatting (Bash/Write/Edit). Tool_result truncation at 4000 chars. `[Error]` prefix on failed tool results. Thinking blocks dropped. Silent skip on non-user/assistant record types.
- PR #123 hotfix — `.jsonl` file upload routing. Web upload handler now distinguishes `.zip`, `.jsonl`, `.json` suffixes. File picker `accept` includes `.jsonl`. Format chip for Claude Code added to import page.

**Context layer live.** Repo now has `CLAUDE.md` (revised) + `context/{soul.md, user.md, llm-config.md}` + `ops/sessions/*.md` + `ops/learned/README.md`. Session writes and reads are part of the agent protocol. Template H v2 (context-layer-aware, ~40% shorter prompts) is the new drafting standard — saved as `context/template-h.md` if committed, otherwise in session output.

**Decisions:**
- Claude Code importer format decisions: thinking blocks dropped (model-internal, noise for the reader), truncation at 4000 chars (p90 of real tool_result sizes was 7.6K, MCA's 500 was too aggressive), per-tool smart rendering (Write shows file_path, not file content), list-typed tool_result content flattened via same recursive extractor as message content. Verified against 7 real session files before drafting.
- P1 Phase 2 design locked: project-first collapsible tree (not flat 400-row list), native HTML `<details>`/`<summary>` for disclosure, scan function factored into `src/importers/claude_code_discovery.py` as a standalone consumable so P7 (CLI dispatch, next) can wrap it without refactor. Path-override via `?path=` allowed but validated under `Path.home()`. No cap on session count (user triggers scan explicitly; not a startup cost).
- Archive never cascades. FTS untouched by archive. Delete-from-archive is single-surface via existing 3a confirm page — no separate destructive flow on archive page.

**Next:**
- Paste P1 Phase 2 prompt into Claude Code. Prompt file: `prompts/prompt-p1-phase2-claude-code-auto-discovery.md` (or wherever you stored it after the last session output).
- After P1 Phase 2 merges: P7 (CLI subcommand dispatch) is explicitly next.
- Queued: Prompt 3c (per-id summary/distill delete routes), P3 drop folder, companion layer prompts CP1/CP4/CP7/CP8.

**Test suite:** 850+ passing on `main`. Pre-existing failures on `test_licensing.py` and `test_intelligence_provider.py` when `SOULPRINT_DEV_MODE=1` and `SOULPRINT_LLM_BASE_URL` are set in shell env — tracked, not blocking, fix on `fix/test-env-hygiene` branch.

**Open threads (no blockers):**
- None. Clean slate for P1 Phase 2.
