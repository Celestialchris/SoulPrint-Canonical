# Session: April 24, 2026 — PR next-3

## What shipped

- **New route**: `GET /archive/health` in `src/app/__init__.py` (inserted between `home` and `passport_surface`). Renders `verify_archive()` output (5 checks, overall status, counts) plus last-import-per-provider table from `ImportRun`. Read-only, no buttons, no polling.
- **New helper**: `last_import_run_per_provider()` in `src/app/import_runs.py`. Returns dict keyed by all 5 `PROVIDER_DISPLAY_NAMES` IDs; value is the most-recent `ImportRun` row or `None`. Null-provider rows excluded. Single query + Python grouping, no subquery joins.
- **New template**: `src/app/templates/archive_health.html`. Three `surface-card` sections: page header, verify checks list, provider table. `file-proof` footer. No new CSS classes; uses existing `surface-card`, `content-list`, `page-header`, `page-eyebrow`, `page-summary`, `file-proof` only.
- **Sidebar**: Sanctum group in `base.html` now has three entries: Workspace, Ask, Archive health. Active detection: `request.path.startswith('/archive/health')`.
- **Tests**: +10 (7 in `tests/test_archive_health.py`, 3 in `tests/test_import_run.py`). Full suite: 1024 passed.

## Notes

- `test_archive_health_shows_db_path` required normalizing backslashes to forward slashes for comparison: `normalize_sqlite_uri` converts the URI on Windows, so the rendered path uses `/` not `\`.
- `format_timestamp` is passed as a context variable (not a Jinja filter) — template uses function-call syntax `{{ format_timestamp(run.imported_at) }}`.

## What's next

PR next-4: workspace health badge — compact status indicator on the `/` home page linking to `/archive/health` from all three states (healthy, issues, unknown).
