## April 22, 2026 — lxml floor bump (CVE-2026-41066)

**Branch:** fix/lxml-cve-2026-41066

**What shipped:**
Added `>=6.1.0` floor to the `lxml` dependency in `pyproject.toml`. `pip install -e . --upgrade` resolved to lxml 6.1.0. Full suite: 930 passing, no regressions.

**CVE-2026-41066:**
Default XXE in `etree.iterparse` and `ETCompatXMLParser`. SoulPrint's only lxml call site is `BeautifulSoup(html, "lxml")` in `src/importers/gemini.py:370`, which routes through lxml's HTML parser (libxml2 HTML mode), not the XML parser path. The vulnerable functions are not called anywhere in the codebase. Actual exposure is zero; this is a pre-launch hygiene bump.

**Dep-resolver path:**
`google_takeout_parser` is not a declared direct dep in `pyproject.toml` (the prompt assumed it was, but it is an independently installed package). Its own constraint is `lxml>=4.6.0` with no ceiling, so `6.1.0` is fully compatible. No bump to `google_takeout_parser` was required.

**Decisions:**
- Unconstrained `"lxml"` → `"lxml>=6.1.0"`. One character change, no other dep changes.
- Did not add a ceiling — no reason to restrict future lxml minor versions.

**Next:** Push + PR for this branch. Last security prompt.
