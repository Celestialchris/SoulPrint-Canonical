# Session Log, 2026-04-27 (9)

**Branch:** `landing/truth-rewrite`
**Scope:** Align the static landing page (`landing/index.html`) with current product truth (five providers, source-install flow, qualified trust language, attachment-aware exports). Static landing page presentation, reveal animations, smooth scrolling, and the pixel-grid USB visual element are preserved by design — visual coherence with the corrected copy is Branch 4 territory.
**Files edited:** `landing/index.html`, this session log.
**Files NOT edited:** `README.md`, `SECURITY.md`, `ROADMAP.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `CLAUDE.md`, `AGENTS.md`, `DECISIONS.md`, `pyproject.toml`, `LICENSE`, `NOTICE`, `docs/`, `docs/specs/`, `docs/product/`, `context/`, `.claude/`, `ops/learned/`, existing files under `ops/sessions/`, `src/`, `tests/`, `sample_data/`, `scripts/`, `bootstrap/`, `.github/`, `landing/assets/`, package metadata, dependency files, release workflows, license validation logic, CSS outside `landing/index.html`, Jinja templates.

## Verified Facts at session start

After `git checkout main && git pull` (already aligned with `origin/main` since `april-27-2026-8.md` shipped) and `git checkout -b landing/truth-rewrite`, the following stale public claims were all present on `landing/index.html`:

- `<title>SoulPrint — A virtual USB stick for your AI life</title>`.
- `<meta name="description">` mentioned three providers ("ChatGPT, Claude, and Gemini") and used the absolute trust claim "Nothing leaves your machine."
- Nav CTA pointed directly at `https://github.com/Celestialchris/SoulPrint-Canonical/releases/latest/download/SoulPrint-Setup.exe` with label `Download`.
- Hero `<h1>` framed SoulPrint as "A *virtual USB stick* for your AI life".
- Hero sub-headline mentioned three providers only.
- Hero primary CTA was `Download free` linking to the `.exe`.
- Hero trust microcopy said "Everything stays on your machine. Nothing is sent anywhere." (absolute claim, no carve-out for optional intelligence).
- Provider pills row showed three providers plus a "More coming" placeholder.
- Import feature card mentioned three formats.
- Search feature card used vague "lane-aware retrieval" language without naming SQLite FTS5.
- Ask feature card omitted the "configured/optional" qualifier.
- Themes feature card framed the slot around topic detection rather than the more current Continuity / Distill / bridge handoff arc.
- Export feature card mentioned only the Memory Passport.
- Terminal demo block used `pip install -r requirements.txt` and `python -m src.main`, plus illustrative-but-fake exact import counts (`847 conversations`, `41,293 messages`).
- How-it-works step 01 mentioned three providers only.
- Difference "With SoulPrint" column listed three providers only.
- Bottom CTA copy used the USB-stick metaphor "Plug in and own it" and the absolute "No cloud" wording.
- Bottom CTA primary action was `Download free` linking to the `.exe`.

`README.md` confirms current source-install flow (`pip install -e .` then `soulprint`) and a five-provider canonical order (ChatGPT, Claude, Claude Code, Gemini, Grok). `SECURITY.md` confirms the core archive is local but optional intelligence features contact only the configured LLM provider when explicitly triggered. `CHANGELOG.md § Unreleased` records attachment-aware exports as the current export-arc truth. `ROADMAP.md § Parked` keeps "Landing page refresh (soulprint.dev)" as cleanup, not a feature build, and `april-27-2026-8.md` listed "Landing-page truth rewrite. Separate branch." as an explicit followup of the contributor-docs sweep.

## landing/index.html patches

Twelve text-and-link patches inside `landing/index.html`. Embedded CSS (`<style>` block), reveal animations, IntersectionObserver script, four-column section rhythm, and the `.hero-usb` pixel-grid block (lines 159-174) and its `.hero-usb` CSS class were all left intact.

### Title and meta

- `<title>` rewritten from "SoulPrint — A virtual USB stick for your AI life" to "SoulPrint — Your AI conversations, home".
- `<meta name="description">` rewritten to lead with the README "Your AI conversations, home" framing, list all five providers in canonical order, mention search and continuity, and replace the absolute "Nothing leaves your machine" line with "Core archive features stay local on your machine."

### Nav

- Nav CTA link changed from the direct `.exe` URL (`.../releases/latest/download/SoulPrint-Setup.exe`) to the README install anchor (`https://github.com/Celestialchris/SoulPrint-Canonical#install`). Label changed from "Download" to "Install".

### Hero

- `<h1>` rewritten from `A <em>virtual USB stick</em><br>for your AI life` to `Your AI conversations,<br><em>home.</em>`.
- `.hero-sub` paragraph rewritten to list all five providers and to add "Continue old threads. Keep the work on your machine." matching the README "Bring conversations home" + continuity arc.
- `.hero-actions` primary button (`.btn-fill`) text changed from "Download free" to "Install from source" and href changed from the `.exe` URL to the README install anchor. Secondary `.btn-outline` (View on GitHub) unchanged.
- `.hero-trust` microcopy rewritten from the absolute "Everything stays on your machine. Nothing is sent anywhere." to "Core archive features stay local. Optional intelligence only contacts the LLM provider you configure and trigger." Matches `SECURITY.md § Network behavior` and `README.md § Data and trust`.

### Providers row

- Provider pills row replaced. Old shape: three named pills (ChatGPT, Claude, Gemini) plus a "More coming" placeholder. New shape: five named pills in canonical order (ChatGPT, Claude, Claude Code, Gemini, Grok), no placeholder.
- Pill dot colors refreshed from the pre-Quiet-Archive Magenta-Sanctum-era palette (`#4ade80`, `#a78bfa`, `#60a5fa`) to the current Quiet Archive v3 lane palette pulled from `CHANGELOG.md § Unreleased > Provider palette revised` and `.claude/rules/brand.md`: ChatGPT `#23955D`, Claude `#D97757`, Claude Code `#F5C518`, Gemini `#2D6FE8`, Grok `#8B3A3A`. The landing page has its own embedded CSS (no `app.css` import), so inline hex literals follow the existing landing-page convention rather than introducing CSS variables.

### Feature cards

- Import card body rewritten to list all five formats (`.zip`, `.json`, `.jsonl`, Gemini Takeout, `.json`).
- Search card body rewritten to name SQLite FTS5 and the snippet/deep-link behavior matching `README.md § What SoulPrint does > Search exact messages`.
- Browse card unchanged.
- Ask card body rewritten to add the "Optional grounded answering" qualifier and the "configured LLM provider" carve-out, matching the trust language elsewhere on the page.
- Fifth card retitled from "Discover themes" to "Continue old threads" with body rewritten around Distill, Continuity Packets, and bridge handoffs. The README leads with the continuity arc, not topic detection, and "Continue old threads" is the more emotionally resonant public-truth promise.
- Sixth card retitled from "Export passport" to "Export without lock-in" with body rewritten to mention Markdown, attachment-aware zip bundles, the Obsidian raw-inbox bridge, and the Memory Passport. Adds CHANGELOG-Unreleased attachment-aware exports as visible public truth.

### Terminal demo

- Install commands updated: `pip install -r requirements.txt` → `pip install -e .`; `python -m src.main` → `soulprint`. Matches the `[project.scripts]` console script entry and the README install flow.
- Server URL line unchanged (`http://127.0.0.1:5678`).
- Fake illustrative import counts ("Imported 847 conversations with 41,293 messages", "Duplicates skipped: 0") softened to neutral demo wording: "Imported conversations and messages into the local ledger" and "Duplicates skipped automatically". Since this branch was already editing the terminal block's stale install commands, neutralizing the fake exact counts at the same time follows the prompt's "prefer neutral demo wording if editing that block anyway" guidance.
- Search demo lines (Lisbon trip illustrative output) preserved as-is. They are clearly demo content and remain accurate to the cross-provider search story.

### How it works step 01

- Step 01 body updated from "Download your conversation history from ChatGPT, Claude, or Gemini. Each has an export option in settings." to "Download your conversation history from ChatGPT, Claude, Claude Code, Gemini, or Grok. Each has an export option in settings or session files." The "or session files" clause covers the Claude Code `.jsonl` case, which is not an export-from-settings flow.
- Steps 02–04 unchanged.

### Difference grid

- "With SoulPrint" column row 1 updated from "One archive across ChatGPT, Claude, and Gemini" to "One archive across ChatGPT, Claude, Claude Code, Gemini, and Grok".
- All other "Without SoulPrint" and "With SoulPrint" rows unchanged.

### Bottom CTA

- `<h2>` rewritten from "Your AI memory.<br><em>Plug in and own it.</em>" to "Your AI memory.<br><em>Bring it home.</em>". Drops the USB metaphor while keeping the rhythm and emotional weight of the close.
- Trust paragraph rewritten from "Open source. Local-first. Apache-2.0 licensed.<br>No accounts. No cloud. No tracking." to a three-line block that lists what SoulPrint does not have (no accounts, no analytics, no telemetry, no background sync — all sourced from `README.md § Data and trust`) without claiming an absolute "No cloud", and adds a small-print line: "Older packaged builds are on GitHub Releases, but source install has the newest fixes." The packaged-build mention takes the small-print form recommended in the prompt.
- Bottom CTA primary button text changed from "Download free" to "Install from source" and href changed from the `.exe` URL to the README install anchor. Secondary "View on GitHub" button unchanged.

### Footer

- Footer unchanged. Apache-2.0 line and `github.com/Celestialchris/SoulPrint-Canonical` link kept verbatim.

### Script and CSS

- `<style>` block, `<script>` IntersectionObserver, all keyframes (`fadeUp`, `blink`, `typeCursor`), `.reveal` animation timing, and the `.hero-usb` / `.pixel-grid` / `.px-*` rule families left untouched.
- The pixel-grid USB visual block (`<div class="hero-usb">` lines 159-174) was preserved by design. The corrected hero copy now reads "Your AI conversations, home" above a USB-shaped pixel grid; the visual coherence problem (page renders a USB stick while corrected copy says "home") is intentionally deferred to the future SvelteKit / visual rebuild branch (Branch 4).

## Out of scope, intentionally not changed

- `landing/assets/` (no asset changes; existing `assets/logo.svg` favicon link kept).
- `src/app/static/app.css` and any other live-app CSS. The landing page is a separate static document with its own embedded styles.
- Jinja templates (`src/app/templates/**`). Not touched in any way.
- `README.md`, `SECURITY.md`, `ROADMAP.md`, `CHANGELOG.md`. Out of allowed-files set; the corrected landing copy is read-aligned to these files but they are not edited.
- `pyproject.toml`, `requirements*.txt` (none should exist now post-PR-merge), package metadata, license validation logic, release workflows.
- `.claude/`, `context/`, `docs/`, `docs/specs/`, `docs/product/`. Not touched.
- Existing files under `ops/sessions/`. Only `april-27-2026-9.md` (this file) is added.
- No new `ops/learned/` entry. Pattern reused: docs-truth-surface cleanup branch with a session log. No new reusable lesson surfaced.
- `CHANGELOG.md` not updated. The repo's current convention (per `april-27-2026-8.md`) is to skip changelog entries for docs-only or static-landing-only cleanup branches; the current Unreleased entries are about app-surface and contributor-docs changes, not the marketing site. If Chris wants the landing refresh recorded in CHANGELOG, that can be a tiny followup.

## Checks run

```
git branch --show-current
git status --short
git diff --check
grep -nE 'SoulPrint-Setup\.exe|requirements\.txt|python -m src\.main|ChatGPT, Claude, and Gemini|ChatGPT, Claude, Gemini|Nothing is sent anywhere|No cloud|virtual USB stick|Plug in and own it|Download free' landing/index.html
git diff --stat
git status --short
```

Expected and observed:

- Branch before edits: `landing/truth-rewrite`. Working tree clean prior to edits.
- `git diff --check` clean after edits (no whitespace-only or conflict-marker noise).
- The stale-content grep against `landing/index.html` returned no matches, confirming the prior `.exe` link, `requirements.txt`/`python -m src.main` install commands, three-provider-only sentences, "Nothing is sent anywhere" absolute claim, "No cloud" absolute claim, "virtual USB stick" hero phrase, "Plug in and own it" close-CTA phrase, and "Download free" CTA-button label have all been replaced with current public truth.
- `git diff --stat` shows two files: `landing/index.html` and this session log.
- `git status --short` shows the same two files (one modified, one untracked) before staging.

No `pytest` run for this static-landing-only branch. No source code, tests, sample data, package metadata, workflows, or live-app CSS touched.

## Followups not included in this branch

- Visual coherence: the corrected hero copy ("Your AI conversations, home") now sits above a USB-shaped pixel grid because the `.hero-usb` block was deliberately preserved per the Stop condition. The future SvelteKit / Branch 4 design rebuild owns the visual replacement.
- Optional CHANGELOG entry under § Unreleased § Changed for the landing-page truth rewrite, if the repo decides docs-style cleanup branches should be visible in the changelog. Convention so far has been to skip these.
- Possible later sweep: replace the inline hex provider-dot colors with locally-declared CSS custom properties at the top of the `<style>` block, mirroring the Quiet Archive v3 token model. Out of scope for a truth-only branch; deferrable until the Branch 4 rebuild touches the same area.
