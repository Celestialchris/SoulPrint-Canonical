## April 29, 2026 — CodeQL py/path-injection sanitizer at assets.py write sinks
**Branch:** fix/codeql-path-injection-assets
**What:** Inlined the CodeQL-recognized `os.path.realpath` + `str.startswith` sanitizer directly before the `mkdir` and `write_bytes` sinks in `store_asset`, copying the canonical shape verbatim from `src/importers/claude_code_discovery.py:42-53`. Closes alerts #20 (line 77) and #21 (line 78), both HIGH severity `py/path-injection`. Added `tests/test_assets_path_injection.py` with three regression tests.

**Decisions:**
- Inlined at the sink rather than centralizing in `asset_absolute_path`. CodeQL does not follow helpers for `py/path-injection`; the PR #140-#144 saga is the precedent. A factored helper was rejected by analyzer recognition even when logically correct.
- Copied the shape verbatim from `claude_code_discovery.py:42-53`. Adapted only variable names (`home` → `storage_base`, `fullpath` → `target`). No improvements, no factoring, no defensive extras. Shape compliance is the deliverable.
- Extracted `storage_base = _resolve_instance_root(instance_root)` as a local on line 45 so the sink check can name the same value used to compute `abs_path` without a duplicate helper call.
- Kept `_sanitize_filename` and `asset_absolute_path` untouched. The first is defense-in-depth at the input boundary; the second uses `is_relative_to` which is logically fine but CodeQL-unrecognized. The fix is additive at the sink, not a replacement.
- Did not modify the recovered-path block at lines 73-76. CodeQL only flagged the main-path sinks (alerts #20 and #21). If the rescan flags the recovered-path branch on a future run, it gets the same inline shape; for now it stays untouched per the prompt's gating rule.

**Pattern observation:**
- No new entry to `ops/learned/` is warranted. The canonical `realpath + startswith` shape was already shipping verbatim at `claude_code_discovery.py:42-53` (introduced in the PR #126-#130 saga) and is referenced from `ops/learned/static-analyzer-shape-matching.md`. This branch consumed it without modification. The doctrine held: copy verbatim, do not improve.
- One mild documentation gap noticed but not addressed in-branch: `ops/learned/codeql-taint-vs-relative-to.md` documents the url-redirection canonical shape inline, but the path-injection canonical shape lives only in `claude_code_discovery.py` and the `static-analyzer-shape-matching.md` narrative. Future contributors hitting a `py/path-injection` alert may chase the shape across two files. Promoting the shape into `codeql-taint-vs-relative-to.md` would compound knowledge but is outside this branch's scope.

**Tests:** 1149 → 1152 passing (+3, exactly matching the new file). 1 test skipped on Windows (`test_symlink_escape_raises_value_error`) because `os.symlink` requires admin or Developer Mode. CI Linux exercises the real security property.

**Verification:** Local pytest passes. CodeQL rescan happens on PR open; alerts #20 and #21 close on main only after merge plus the next main-branch CodeQL scan. Report-02 logs the routing.

**Next:** Open PR. Wait for CodeQL run on the PR diff. If the analyzer still flags the touched sinks, STOP per the prompt's stop conditions and do not iterate blindly. After merge, confirm alerts #20 and #21 close on main.
