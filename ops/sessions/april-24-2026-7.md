# Session log: 2026-04-24 (PR next-4 — Workspace health badge)

## New function

`quick_health_summary(db_path: Path) -> dict` appended to `src/verify.py`.
Three states: "healthy" / "needs_attention" / "unknown". Skips
`PRAGMA integrity_check` and orphan scan. Reuses `CORE_TABLES`,
`FTS_TABLES` constants and sqlite3 try/finally pattern from `verify_archive`.

## home() extension

Added module-level `from ..verify import quick_health_summary` import after
the `..passport` import block. `home()` now computes `health_summary` and
`badge_label` (from a local dict inline in the route) and passes both to
`render_template("index.html", ...)` as additive kwargs.

## Template injection

Badge anchor injected at top of `{% block right_panel %}` in `index.html`,
immediately before `<div class="context-panel__header">Archive status</div>`.
Links to `/archive/health` via `url_for('archive_health')`.
BEM modifier set from `health_summary.state` with `replace('_', '-')` filter.

## CSS block

Scoped `.workspace-health-badge*` block (~25 lines) appended to end of
`app.css`. No existing rule or token definition touched. All tokens resolve
under both themes via the `[data-theme]` override block already in the file.

## Test delta

+8 tests: 4 in `tests/test_verify.py` (quick_health_summary unit tests),
4 in `tests/test_workspace_home.py` (badge rendering + link tests).
Full suite: 1032 passed.

## Observable Archive v0 complete

PRs next-1 through next-4 ship the full Observable Archive v0 milestone:
- next-1: `soulprint verify` CLI + `src/verify.py` module
- next-2: ImportRun history table + classify_import_outcome
- next-3: `/archive/health` page with full verify output + last-import-per-provider
- next-4: Workspace health badge (this PR) — glance-level signal linking to the truth
