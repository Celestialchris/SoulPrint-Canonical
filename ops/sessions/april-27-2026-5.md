# Session: April 27 2026 - docs/spec-status-banners

## Branch
`docs/spec-status-banners`

## What shipped
Spec status banners on partially shipped specs, deletion of an obsolete one-line stub, Claude Code provider-reference fix in the raw-inbox guide, and a new learned-pattern file documenting the typography self-reference trap.

No runtime code, CSS, model, route, or product-doc changes on this branch.

## Files changed

- `docs/specs/session-attachments-spec.md`: replaced `Status: draft 0` header line with a status banner stating Phases 1 through 4 shipped (Asset/ConversationAsset/MessageAsset models, attachment-aware export bundles), pointing readers at `src/app/models/__init__.py` and `src/app/__init__.py` for current shape, and citing the cumulative session logs.
- `docs/specs/tagging-spec.md`: added a 2026-04-27 note under "Current state audit" clarifying that the audit and the "Gap this spec fills" subsection describe the pre-implementation state. The MVP shipped 2026-04-22 per the existing file header.
- `docs/specs/obsidian-bridge-spec.md`: added a conservative status banner near the top describing the ambiguity between the structured Chats/Themes/Daily layout in this spec and the simpler raw-inbox path documented in `docs/obsidian-raw-inbox.md`. Updated `Vault Layout` provider examples and the `Provider refs` line to include `ClaudeCode.md` and `Grok.md` alongside the existing `ChatGPT.md`, `Claude.md`, `Gemini.md`.
- `docs/specs/soulprint-expansion-plan.md`: replaced the stale planning-draft and Phase 11 precondition header lines with a 2026-04-27 banner. Banner names P1 (Claude Code session auto-discovery, importer plus discovery helper plus `/imported/scan-claude-code` route plus `soulprint scan claude-code` CLI verb) and P7 (CLI dispatch via `pyproject.toml` console script `soulprint = "src.cli:main"`) as shipped, and notes that P3, P4, P5, P6 are not verified from docs alone. The Philosophy line was preserved untouched.
- `docs/specs/soulprint-companion-layer-plan.md`: replaced the stale planning-draft and Phase 11 precondition header lines with a 2026-04-27 banner. Banner names CP1 (PR #139 starring), CP2 (archive/hide), CP3 (tags MVP), CP6 (open-loops command center at `/continuity/open-loops`), and CP11 (`ImportRun` plus `/archive/health`) as shipped, and notes the remaining CP items remain queued design records. The Philosophy line was preserved untouched.
- `docs/specs/next.md`: deleted via `git rm`. The file was a single-line obsolete stub naming CP1 starring, CP4 Continue in X, CP7 Wrapped v2, CP8 Persona Extract.
- `docs/obsidian-raw-inbox.md`: updated the "Who this is for" sentence from "ChatGPT, Claude, Gemini, and Grok" to "ChatGPT, Claude, Claude Code, Gemini, and Grok". Inserted `claude_code -> [[ClaudeCode]]` (rendered with U+2192 in the file) into the wiki-link mapping line after the `claude` entry. The U+2192 rightward arrow was preserved in this file because the U+2014 sweep does not flag it.
- `ops/learned/typography-self-reference.md`: new learned-pattern file. Documents the trap pattern that surfaced in Branch 1 (`docs/public-docs-truth-cleanup`, commit `b6cf619`): when a docs branch enforces a rule against a specific glyph, the session log and the learned-pattern file itself must describe the glyph semantically rather than quote it, because a literal quote silently reintroduces the very artifact the rule removes. Lists three semantic substitutions and a verification grep command.
- `ops/sessions/april-27-2026-5.md`: this file.

## Status banners added

Five spec files received status labeling:

1. `session-attachments-spec.md`: header banner naming Phases 1 to 4 as shipped.
2. `tagging-spec.md`: in-section note clarifying the audit describes pre-implementation state.
3. `obsidian-bridge-spec.md`: conservative banner naming the structured-vs-raw-inbox ambiguity.
4. `soulprint-expansion-plan.md`: header banner with P1 and P7 marked shipped, P3/P4/P5/P6 unverified.
5. `soulprint-companion-layer-plan.md`: header banner with CP1, CP2, CP3, CP6, CP11 marked shipped, remaining CP items queued.

## docs/specs/next.md deletion

Performed. The file was the obsolete single-line stub described in the Verified Facts section of the prompt. No other content. Deletion staged via `rtk git rm`.

## docs/obsidian-raw-inbox.md provider-reference fix

Two edits, both narrow:

1. Provider list near the top now includes Claude Code.
2. Wiki-link mapping line now includes `claude_code -> [[ClaudeCode]]` (rendered with the same U+2192 rightward arrow already used in that line).

No other edits to the file.

## ops/learned/typography-self-reference.md addition

New file. Captures the pattern Chris and the Branch 1 work surfaced: documenting a rule against a glyph requires not typing that glyph in the documentation. References Branch 1 (`docs/public-docs-truth-cleanup`, commit `b6cf619`). Contains zero instances of the forbidden U+2014 glyph by design.

## Checks run

- `rtk git diff --check`: no whitespace errors.
- Literal U+2014 sweep on the new artifacts (the learned-pattern file and this session log): zero matches. The sweep was scoped to the two new files only because spec banner content and the raw-inbox guide are not public copy and may legitimately retain pre-existing U+2014 occurrences.
- Stale provider phrase check on `docs/specs/obsidian-bridge-spec.md` and `docs/obsidian-raw-inbox.md`: zero matches for the three-provider phrase pattern that signals an old `ChatGPT, Claude, Gemini` listing.
- `rtk git diff --stat` and `rtk git status --short`: scope confined to the eight allowed files plus the deletion of `docs/specs/next.md`.

No pytest run. Docs-only branch.

## Followups not included in this branch

- Branch 2B: product-doc cleanup for `docs/product/brand.md`, `docs/product/positioning.md`, `docs/product/landscape.md`. Editorial judgment work; deferred from this mechanical labeling pass.
- Branch 3: landing-page truth rewrite.
- Post-loopback SECURITY/ROADMAP caveat cleanup if still needed once the loopback guard branch (PR #178) settles.
- Contributor-docs sweep for stale provider/sample-data references (CONTRIBUTING.md and friends).

## Existing session logs

Not modified. Existing logs under `ops/sessions/` are immutable history per the prompt and per the repo policy. The only new file under `ops/sessions/` is this session log.
