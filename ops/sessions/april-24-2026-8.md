# Session — April 24 2026 · chore/archived-tabs-five-providers

## What was done

Added Claude Code and Grok to the archived-view tab row in `imported_archived.html`. The row previously hardcoded only ChatGPT, Claude, and Gemini (three tabs). PR #145 had fixed this for `/imported` but missed `/imported/archived`.

## Files changed

- `src/app/templates/imported_archived.html` — added two `<a>` elements for `claude_code` and `grok` inside `.provider-tabs` nav block, in canonical `SUPPORTED_IMPORT_PROVIDERS` order. Labels use `{{ 'slug' | provider_display_name }}` filter, not hardcoded strings. CSS classes use hyphens (`provider-tab--claude-code`), query-params use underscores (`?provider=claude_code`).
- `tests/test_imported_archive.py` — added `ArchivedProviderTabsTest` class with 4 test methods covering: all-five-tabs render, All-tab active on no provider, claude_code tab active state, grok tab active state.
- `CHANGELOG.md` — `### Fixed` entry under `[Unreleased]`.

## Test result

1040 passing (was 1033 on main at prompt time; main pulled in 7 additional tests from the `fix/archive-health-pre-instrumentation-empty-state` PR that merged between prompt authoring and execution). All new tests pass.

## PR

https://github.com/Celestialchris/SoulPrint-Canonical/pull/158
