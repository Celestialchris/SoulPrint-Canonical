# Session log: 2026-04-27 (4)

## Branch

`docs/public-docs-truth-cleanup`

Branched from a clean `main` at `008d29f Restore product coherence doctrine`.

## Files changed

- `docs/getting-started.md` (rewritten)
- `docs/manifesto.md` (surgical patches, voice preserved)
- `privacy.md` (rewritten)
- `docs/README.md` (specs index updated, future-directions pointed at v2, one stray em dash converted to a colon)
- `ops/sessions/april-27-2026-4.md` (this log, new)

No existing session logs were modified. Existing logs are immutable history.

## Stale claims removed

From `docs/getting-started.md`:

- `SoulPrint-Setup.exe` Windows installer instructions.
- `SoulPrint-macos.zip` + `~/Applications/` install path.
- `SoulPrint-linux.tar.gz` + `~/.local/share/SoulPrint/` install path.
- `bootstrap/install_windows.bat` / `bootstrap/install_macos.sh` / `bootstrap/install_linux.sh` references.
- `pip install -r requirements.txt` (file removed at commit `042bc36`).
- `python -m src.main` "alternative" launch line (kept `soulprint` console script as the canonical entrypoint).
- `scripts\build_windows.bat` Windows Executable section.
- Sample-data list that named only ChatGPT, Claude, and two Gemini fixtures (now lists ChatGPT, Claude, Claude Code, Gemini, Grok).

From `docs/manifesto.md`:

- "ChatGPT, Claude, and Gemini" three-provider claim.
- "without making a single network call" absolute claim.
- "Local by architecture ... only network calls happen ... using your own API key" framing that ignored Ollama.
- "Intelligence without surveillance ... computation happens through your own API key" framing that ignored the keyless local path.

From `privacy.md`:

- Unqualified "no outbound network calls" claim.
- Implicit framing that intelligence is OpenAI/Anthropic only.
- Windows installer + `%LOCALAPPDATA%\SoulPrint\` filesystem claim that is not backed by a current published installer.

From `docs/README.md`:

- Specs index that was missing `frontend-evolution-doctrine`, `session-attachments-spec`, `tagging-spec`, `soulprint-companion-layer-plan`, and `soulprint-expansion-plan`.
- One em-dash separator in the Guides section between the `obsidian-raw-inbox.md` link and its caption, converted to a colon.

## Current truths used

Verified against the live repo before editing:

- Install path is `pip install -e .` (`README.md` § Install, `pyproject.toml`).
- Console script is `soulprint = "src.cli:main"` (`pyproject.toml` § project.scripts).
- Intelligence extras come from `pip install -e ".[intelligence]"` (`pyproject.toml` § project.optional-dependencies).
- Supported providers are ChatGPT, Claude, Claude Code, Gemini, Grok (`README.md` § Providers, `CHANGELOG.md` Unreleased).
- `requirements.txt` no longer exists (`CHANGELOG.md` Unreleased § Removed).
- Core archive (import, browse, search, notes, exports, passport, MCP, answer-trace browser) makes no network calls beyond loopback (`SECURITY.md` § Network behavior).
- Intelligence features call only the configured provider, only when triggered (`SECURITY.md` § LLM provider behavior).
- Local OpenAI-compatible (Ollama) mode is keyless and stays on the machine (`SECURITY.md` § LLM provider behavior; `README.md` § Optional: fully local intelligence).
- Cloud providers (OpenAI, Anthropic) are BYOK (`SECURITY.md` § LLM provider behavior).
- MCP server speaks to a local client over stdio and does not open a network port (`SECURITY.md` § MCP and local server exposure).
- `sample_data/` contains exactly the seven fixtures listed in Verified Facts (verified by directory listing).

## Checks run

```
git status (clean before, scoped diff after)
git branch --show-current (docs/public-docs-truth-cleanup)
git diff --check (no whitespace issues)
git diff --stat (4 files changed, scoped)
grep -nE 'requirements\.txt|SoulPrint-Setup\.exe|ChatGPT, Claude, and Gemini|ChatGPT, Claude, Gemini([^,]|$)|no outbound network calls|no network calls' docs/getting-started.md docs/manifesto.md privacy.md docs/README.md (no matches)
grep for the em-dash character across the four files (no matches)
```

The full Python test suite was not run; this is a docs-only branch and the doctrine in `CLAUDE.md § Git Workflow` covers code/UI changes, not Markdown copy. If repo CI policy requires it on docs-only PRs, the suite can be run before merge.

## Followups not included in this branch

These were noted during this cleanup but are explicitly out of scope:

- `landing/index.html` truth rewrite (separate branch).
- `docs/specs/` content cleanup, including resolving the role of `docs/specs/next.md` (separate branch). The index now omits `next.md`; the file itself was not deleted.
- `docs/product/` provider/status cleanup (separate branch).
- `docs/architecture/` and `docs/releases/` review.
- Removing the loopback caveat in `SECURITY.md` § LLM provider behavior + the matching `ROADMAP.md` entry, once the loopback guard work has soaked.
- README CONTRIBUTING.md cross-reference points to a file that should be confirmed to exist and be current; not verified in this branch.

## Confirmation

Existing session logs under `ops/sessions/` were not edited or rewritten in this branch.
