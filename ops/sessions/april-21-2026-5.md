## April 21, 2026 — CP5 followup: NULL-timestamp demotion, URL encoding, learned pattern

**Branch:** fix/cp5-archaeology-followups (PR #136)
**Version:** v0.7.0-alpha.3

**What shipped:**
- PR #134 — Provenance Narrative: search callout (R1-R5 with archaeology no-op) + Wrapped origin block (R6-R8). 886 tests.
- PR #135 — CP5 Conversation Archaeology: `sort=relevance|newest|oldest` on `search_fts`, `?mode=archaeology` UI activating R5 suppression. 908 tests.

**Reviews on PR #135 (post-merge):**
- Internal 12-reviewer run: 1 P1 (NULL/empty timestamp sorts as "oldest" in archaeology), 5 P2s (FTS-failure vs true-empty indistinguishability, SQL duplication, FTSSortTest missing notes, `?sort=newest&mode=archaeology` override test, rank type change advisory), 2 P3s (URL encoding, `.sort-toggle` unstyled).
- Cross-reviewer agreement on NULL-timestamp bug and URL encoding. High confidence.

**Decisions:**
- Followup PR scope: NULL-timestamp demotion, URL encoding, two test gaps, learned-pattern entry, session log. Deferred: `.sort-toggle` CSS, FTS-failure distinguishability, MCP `soulprint_search` sort parity (CP5.1), pre-existing `{{ snippet | safe }}` XSS.
- Task 1 restructured as test-first per `llm-hygiene.md`. Failing test seeds an FTS row directly with `timestamp=""` (simulating `_format_unix_ts(None)`). `MemoryEntry.timestamp` has NOT NULL constraint — can't seed via ORM. Direct FTS insert is the correct approach.
- Regression-guard test on newest mode verifies DESC lexicographic ordering demotes empty strings naturally. That test passed without code change, confirming sentinel only needed for ASC.
- conn.close() fix (P1 from review) carried forward from stale working tree and included in PR #136 commit #2.
- Learned-pattern convention: standalone file with README index pointer.
- Archive checkpoint deferred: `/mnt/user-data/uploads/future-directions.md` path not found on this machine; no paste provided.

**Next:**
- Candidate queue: landing page refresh, companion layer CP1/CP4/CP7, MCP `soulprint_search` sort parity (CP5.1), `.sort-toggle` CSS.

**Test suite:** 908 → 914, all passing.

**Open threads (no blockers):** None.
