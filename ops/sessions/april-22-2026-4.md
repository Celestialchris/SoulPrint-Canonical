## April 22, 2026 — CodeQL canonical sanitizer shape (fix/safe-next-canonical-pattern)
**Branch:** fix/safe-next-canonical-pattern
**What:** Replaced the factored `parsed`/`is_safe`/ternary inline block at all four star/unstar redirect sinks with CodeQL's exact canonical pattern (`replace("\\", "")` mutation + inline double `urlparse(nxt).netloc` / `urlparse(nxt).scheme` guard + early-return). Updated `test_star_rejects_bad_next` in both `StarImportedRouteTest` and `StarMemoryRouteTest` to assert browser-level safety instead of literal string containment.
**Decisions:**
- Alerts #12-15 closed after the previous inlining pass, but CodeQL spawned #16-19 on the IfExp ternary. Root cause: factoring through `parsed = urlparse(nxt)`, `is_safe = bool(...)`, and `redirect(nxt if is_safe else ...)` are all opaque to the taint tracker even at the sink. CodeQL requires the idiom verbatim from its own docs.
- Semantic shift from reject-on-backslash to strip-then-validate: `\\evil.com` strips to `evil.com` and `/\evil.com` strips to `/evil.com`. Both are browser-safe same-origin paths (browser treats `evil.com` as relative path, stays on-host). The previous reject-on-backslash was cosmetically cleaner (no `evil.com` string in Location) but offered no additional browser-level protection.
- Tests updated to reflect real safety property. `assertNotIn("evil.com", location)` and `assertIn("/federated", location)` removed from the subTest loop. Replaced with four `assertFalse(location.startswith(...))` checks covering protocol-relative, http, https, and javascript schemes. The `assertNotIn("javascript", location)` assertion was retained as it passes for all 7 inputs with the new pattern.
- Stop condition in the prompt fired: the test structure had three containment assertions in the subTest body, not one as assumed by the "1-for-4 swap" description. Reported and resolved with explicit direction before proceeding.
- `_safe_next` helper remains defined and unused. Removal deferred to next cleanup pass.
**Next:** Push branch and open PR. Then evaluate #16-19 dismissal vs. wait for CodeQL scan confirmation.
