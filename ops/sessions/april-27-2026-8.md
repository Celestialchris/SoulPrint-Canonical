# Session Log, 2026-04-27 (8)

**Branch:** `docs/contributor-docs-sweep`
**Scope:** Align contributor-facing documentation in `CONTRIBUTING.md` and `CLAUDE.md` with current repo state after the provider, entrypoint, and test-suite count changes.
**Files edited:** `CONTRIBUTING.md`, `CLAUDE.md`, this session log.
**Files NOT edited:** `README.md`, `SECURITY.md`, `ROADMAP.md`, `CHANGELOG.md`, `pyproject.toml`, `LICENSE`, `NOTICE`, `AGENTS.md`, `DECISIONS.md`, `docs/`, `docs/specs/frontend-evolution-doctrine.md`, `landing/`, `src/`, `tests/`, `sample_data/`, `scripts/`, `bootstrap/`, `.github/`, `context/`, `.claude/`, existing files under `ops/sessions/`, `ops/learned/`, CSS, Jinja templates, package metadata, dependency files, release workflows, license validation logic.

## Verified Facts at session start

After `git checkout main && git pull` (already up to date with `origin/main`), the four stale strings called out in the Verified Facts block were all present on `main`:

- `CONTRIBUTING.md` Repo Map line: `tests/              50+ test files, 600+ test methods`.
- `CONTRIBUTING.md` Repo Map line: `sample_data/        Provider fixtures (ChatGPT, Claude, Gemini)`.
- `CONTRIBUTING.md` Running Tests paragraph: `50+ test files (600+ methods)`.
- `CLAUDE.md` Commands block: `python -m src.main                  # run the app` and `python -m pytest tests/ -v          # run tests (700+, all must pass before commit)`.

`README.md` confirms the current source install flow ends in the `soulprint` console script, and `pyproject.toml` defines `soulprint = "src.cli:main"`. `CHANGELOG.md` § Unreleased Tests records `Suite at 1126 passing`, which is the source-of-truth count this branch quotes.

## CONTRIBUTING.md patches

Three contributor-doc truth patches in the Reference section:

- Repo Map test-count line changed from `tests/              50+ test files, 600+ test methods` to `tests/              Test suite, currently 1126 passing`.
- Repo Map sample-data provider line changed from `sample_data/        Provider fixtures (ChatGPT, Claude, Gemini)` to `sample_data/        Provider fixtures (ChatGPT, Claude, Claude Code, Gemini, Grok)`. This brings the line into parity with the README provider list and with `SUPPORTED_IMPORT_PROVIDERS` in `src/importers/contracts.py`.
- Running Tests paragraph rewritten: `Test suite currently records 1126 passing tests, covering import, persistence, retrieval, search, intelligence, distillation, continuity, passport, freemium gate, CLI, attachments, and browser integration.` Adds `attachments` to the coverage list because attachment-aware exports landed under § Unreleased and are part of the suite the count refers to.

The Building for release block, the test command itself (`python -m pytest tests/ -v`), the Rules section, the Surfaces table, and the CLI reference were not touched.

## CLAUDE.md patches

Two command-block truth patches in § Commands:

- `python -m src.main                  # run the app` replaced with `soulprint                            # run the app`. Matches the `[project.scripts]` console script in `pyproject.toml` and the install flow in `README.md`. The legacy module-launch path stays referenced only in `CHANGELOG.md` historical entries (out of scope for this branch).
- `python -m pytest tests/ -v          # run tests (700+, all must pass before commit)` replaced with `python -m pytest tests/ -v          # run tests (1126 passing as of latest changelog entry)`. The pytest invocation itself is unchanged; only the count comment moves to the current source-of-truth wording.

The rest of the Commands block (importer CLI, passport CLI, retrieval CLI, answering CLI) and every other CLAUDE.md section (What SoulPrint Is, Architecture, Patterns, Trust Chain, Anti-Patterns, Terminology, Git Workflow, Product Context) were not touched.

## Out of scope, intentionally not changed

- `docs/specs/frontend-evolution-doctrine.md` status banner. Spec/doctrine surfaces stay in their own cleanup branch.
- `README.md` rewrites. The current README provider list and install flow already match the corrected contributor docs, so no change is needed in this branch.
- `SECURITY.md`, `ROADMAP.md`, `CHANGELOG.md`. Out of allowed-files set.
- Source code, tests, sample data fixtures, CSS, Jinja templates.
- `pyproject.toml` and any package metadata, dependency, or release workflow files.
- License validation logic.
- Existing `ops/sessions/` files and any `ops/learned/` files. No new learned-pattern file was created because this branch reused the existing docs truth-surface cleanup pattern and did not surface a new reusable lesson.

## Checks run

```
git branch --show-current
git status --short
git diff --check
grep -nE 'ChatGPT, Claude, Gemini|50\+ test files|600\+ methods|700\+|python -m src\.main' CONTRIBUTING.md CLAUDE.md
git diff --stat
git status --short
```

Expected and observed:

- Branch before edits: `docs/contributor-docs-sweep`. Working tree clean.
- `git diff --check` clean after edits.
- The grep sweep against `CONTRIBUTING.md` and `CLAUDE.md` returns zero matches for the stale provider list, stale test counts, and stale launch command.
- `git diff --stat` shows three files: `CONTRIBUTING.md`, `CLAUDE.md`, and this session log.
- `git status --short` shows the same three files modified or added before staging.

No pytest run for this docs-only branch. No code, tests, package metadata, or workflows touched.

## Followups not included in this branch

- `docs/specs/frontend-evolution-doctrine.md` status banner. Separate spec-cleanup branch.
- Landing-page truth rewrite. Separate branch.
- No new `ops/learned/` entry. Pattern reused, no new lesson surfaced.
