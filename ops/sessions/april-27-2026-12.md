# Session Log, 2026-04-27 (12)

**Branch:** `site/sveltekit-foundation`
**Scope:** SvelteKit + TypeScript marketing-site foundation only. Establish a top-level `site/` directory as the future runway for `soulprint.dev` without touching the live `landing/` deploy, the Flask app, the test suite, or any documentation surface.

**Files added:**
- `site/.gitignore`
- `site/.npmrc`
- `site/.vscode/` (scaffold default, editor convenience only)
- `site/README.md`
- `site/package.json`
- `site/pnpm-lock.yaml`
- `site/svelte.config.js`
- `site/tsconfig.json`
- `site/vite.config.ts`
- `site/src/app.d.ts`
- `site/src/app.html`
- `site/src/lib/index.ts`
- `site/src/lib/assets/favicon.svg`
- `site/src/routes/+layout.svelte`
- `site/src/routes/+layout.ts`
- `site/src/routes/+page.svelte`
- `site/static/robots.txt`
- `ops/sessions/april-27-2026-12.md` (this log)

**Files NOT edited:** `landing/` (entire tree, including `landing/index.html`), `netlify.toml`, `.github/workflows/` (any file), `src/` (the Flask app), `tests/`, `sample_data/`, `scripts/`, `bootstrap/`, `README.md`, `SECURITY.md`, `ROADMAP.md`, `CHANGELOG.md`, `CONTRIBUTING.md`, `CLAUDE.md`, `AGENTS.md`, `DECISIONS.md`, `pyproject.toml`, `LICENSE`, `NOTICE`, `docs/`, `docs/specs/`, `docs/product/`, `context/`, `.claude/`, `ops/learned/`, all existing `ops/sessions/*.md`, the repo-root `.gitignore`, `tools/ecc/`, package metadata outside `site/`, Python dependency files, release workflows, license validation logic, CSS outside `site/`, and Jinja templates.

## Verified facts at session start

After `git checkout main && git pull` and `git checkout -b site/sveltekit-foundation`, the four expected starting conditions all held on `main`:

- The latest existing session log on `main` was `ops/sessions/april-27-2026-11.md` (PR #186 closeout, dead Pages workflow deletion). Next unused integer for today's date is `12`, so this log is `april-27-2026-12.md`.
- `netlify.toml` at the repo root continued to publish `landing` (`[build] publish = "landing"`).
- `landing/index.html` was the truth-corrected static page from PR #185 / session 10, with the five-provider list (ChatGPT, Claude, Claude Code, Gemini, Grok), source-install CTA, qualified local-first language, and the visual-transitional pixel-grid USB hero still in place. Untouched in this branch.
- No top-level `site/` directory existed on `main`. No top-level `package.json`, `pnpm-lock.yaml`, or `package-lock.json` existed at the repo root. The only Node packaging in the repo lives under `tools/ecc/`, which is unrelated tooling and was not modified.

Working tree was clean before edits.

## Doctrine boundary

`site/` is the marketing-site foundation, not the Phase 3 cockpit from `docs/specs/frontend-evolution-doctrine.md`. The cockpit is gated behind Phase 2 JSON endpoints, which have not shipped. This branch deliberately:

- does not read from the canonical SQLite ledger
- does not call any Flask endpoint
- does not introduce a search UI, transcript explorer, provider filters, or API client
- does not introduce Tailwind, shadcn, animations, hero imagery, app screenshots, or font bundling
- does not migrate design tokens from the Flask app's `app.css`

The site/ tree is a flat marketing surface: one prerendered `+page.svelte` for `soulprint.dev`, no runtime integration with the SoulPrint app.

## Deployment decision

No cutover. The old static `landing/` continues serving `soulprint.dev` via Netlify. `netlify.toml` was not edited; `[build] publish = "landing"` remains the live deploy contract. A separate, explicit branch will own the future cutover from `landing/` to `site/` (or to a built artifact under `site/build/`). This branch only puts the foundation in place.

## Stack and toolchain

- **Framework:** SvelteKit 2.58.0
- **Language:** TypeScript 6.0.3
- **Adapter:** `@sveltejs/adapter-static` 3.0.10 (the scaffold's default `@sveltejs/adapter-auto` was swapped out before the first install, so adapter-auto never made it into `pnpm-lock.yaml`)
- **Compiler:** Svelte 5.55.5 with runes mode forced for project files (default from the `sv` scaffold)
- **Build tool:** Vite 8.0.10
- **Type checker:** svelte-check 4.4.6
- **Package manager:** pnpm 10.33.2
- **Lockfile:** `site/pnpm-lock.yaml` (single lockfile; no `package-lock.json` committed)

The chosen stack matches the future-stage doctrine in `docs/specs/frontend-evolution-doctrine.md`: SvelteKit + TypeScript + strict design tokens posture, with the desktop shell (Tauri) still deferred. Astro and Eleventy would be better marketing-site-fit alternatives in isolation, but doctrine consistency wins here so future cockpit and marketing surfaces share runtime, build tooling, and component conventions.

## Scaffold notes

- The Svelte CLI used was `sv@0.15.1`. The non-interactive flags resolved without falling back to deprecated `create-svelte`.
- Scaffold command actually run: `npx --yes sv@latest create site --template minimal --types ts --no-add-ons --no-install`. The `sv` CLI's recommended replay command was logged inside the project as `npx sv@0.15.1 create --template minimal --types ts --no-install site`.
- `pnpm` was not initially available on this Windows host. `corepack enable` failed with `EPERM` against `C:\Users\chr\AppData\Local\nvm\v24.14.0\yarnpkg` (Windows nvm without elevated shims). Fallback: `npm install -g pnpm`, which succeeded and gave pnpm 10.33.2.
- The scaffold's `+layout.svelte` imports `$lib/assets/favicon.svg`, which is the default Svelte logo SVG. It was left in place. Foundation-stage; brand favicon is a follow-up, not in scope here.
- The scaffold's `.gitignore` already covered `node_modules`, `/.svelte-kit`, `/build`, `.env`, and `.env.*` plus the standard SvelteKit `.output`, `.vercel`, `.netlify`, `.wrangler` entries. It met the prompt's minimum requirement and was left untouched.
- The repo-root `.gitignore` was not edited. Nothing in the scaffold required changes outside `site/`.

## Homepage content

`site/src/routes/+page.svelte` is the simple marketing foundation:

- H1: `SoulPrint: Your AI conversations, home.`
- Lede: SoulPrint is a local-first memory archive for AI conversations; imports run into a private SQLite ledger on the user's machine.
- Supported providers list in canonical order: ChatGPT, Claude, Claude Code, Gemini, Grok.
- "What it does" section: browse, search, continue, export with provenance (and the Memory Passport).
- "Trust posture" section: qualified local-first language. Core archive features stay local; optional intelligence only contacts the LLM provider the user has configured and triggers.
- Two CTAs: primary `Install from source` to `https://github.com/Celestialchris/SoulPrint-Canonical#install`, secondary `View on GitHub` to the repo root.
- Footer line: Apache-2.0; no accounts, no analytics, no telemetry, no background sync.

Styling is intentionally minimal: scoped `<style>` block, system font stack, parchment-ish background, two button variants, no Tailwind, no shadcn, no animations, no fonts loaded over the network, no hero, no USB visual, no screenshots, no theme system. Foundation stone, not the cathedral window. Brand polish, design-token migration, and visual identity are explicit follow-ups on a later branch.

## Static-output configuration

- `site/svelte.config.js` uses `@sveltejs/adapter-static` with `pages: 'build'`, `assets: 'build'`, `fallback: undefined`, `precompress: false`, `strict: true`. `strict: true` is the doctrine guardrail: any future route that is not prerenderable will fail the build instead of silently degrading the static contract.
- `site/src/routes/+layout.ts` exports `prerender = true` and `ssr = true`, so every route under the layout is prerendered to flat HTML by default.
- The build wrote a 5,812-byte `site/build/index.html` plus `site/build/_app/...` immutable bundles and the carried-over `site/build/robots.txt`. The marketing copy and `<title>`/`<meta description>` are present directly in the rendered HTML, so non-JavaScript clients still see the full page.

## Commands run

- `git status --short` → clean before branching (only the pre-existing `ops/sessions/april-27-2026-11.md` showed as the latest log).
- `git checkout -b site/sveltekit-foundation` → succeeded; on the new branch.
- `node --version` → `v24.14.0`. `npm --version` / `npx --version` → `11.11.1`. `pnpm --version` → `command not found` initially.
- `corepack enable` → failed with `EPERM` on the nvm `yarnpkg` shim. Documented as the reason for the `npm install -g pnpm` fallback.
- `npm install -g pnpm` → installed pnpm 10.33.2 globally.
- `npx --yes sv@latest create site --template minimal --types ts --no-add-ons --no-install` → produced the SvelteKit scaffold under `site/` with TypeScript on, no add-ons, and no install pass.
- `pnpm install` (run inside `site/`) → resolved 87 packages, downloaded 62, added 56, generated `site/pnpm-lock.yaml`. The `prepare` hook (`svelte-kit sync`) ran cleanly. Final dev dependencies: `@sveltejs/adapter-static` 3.0.10, `@sveltejs/kit` 2.58.0, `@sveltejs/vite-plugin-svelte` 7.0.0, `svelte` 5.55.5, `svelte-check` 4.4.6, `typescript` 6.0.3, `vite` 8.0.10. No `package-lock.json` produced.
- `pnpm build` → SvelteKit + Vite production build. Wrote 5,812-byte prerendered `index.html`, immutable JS/CSS bundles under `_app/`, and copied `robots.txt`. `Using @sveltejs/adapter-static` reported. ✔ done.
- `pnpm check` (svelte-check) → `166 FILES 0 ERRORS 0 WARNINGS 0 FILES_WITH_PROBLEMS`.
- `git diff --check` → clean (no whitespace or conflict-marker noise; this branch is purely additive under `site/` plus this log).

No `pytest` run. No Python file was touched. No file under `src/`, `tests/`, `sample_data/`, `landing/`, `netlify.toml`, `.github/`, `docs/`, `context/`, or existing `ops/sessions/*.md` was modified. The forbidden-paths diff is empty.

## What this branch closes

This branch closes the SvelteKit-foundation slot in the four-branch frontend evolution sequence: it gives `soulprint.dev` a future home in the same stack as the eventual cockpit, without committing to any deployment cutover and without disturbing the working `landing/` deploy.

It does not close any open `landing/` work. The pixel-grid USB hero in `landing/index.html` is still visually transitional; whether to evolve the static landing page further or cut over to `site/build/` once it has proper visual identity is a separate decision on a separate branch.

## Followups not included in this branch

- Brand identity for `site/`: replace the default Svelte logo favicon with a SoulPrint mark, define and consume design tokens consistent with `src/app/static/app.css`, and pull color and typography decisions from the Quiet Archive doctrine. Owned by a later `site/branding` branch.
- Full visual fidelity for the homepage: layout, type scale, hero treatment, provider lane stripes, and any imagery. Explicitly out of scope for the foundation.
- Deployment cutover from `landing/` to `site/build/`. Requires editing `netlify.toml` (or the Netlify dashboard) and is its own branch with its own verification of the built artifact serving correctly.
- Any CI workflow that builds `site/` on push. Not added here. The current commit lists no `.github/workflows/` change.
- Eventual API surface that the cockpit (Phase 3) will consume. Still gated behind Phase 2 JSON endpoints from `docs/specs/frontend-evolution-doctrine.md`.
