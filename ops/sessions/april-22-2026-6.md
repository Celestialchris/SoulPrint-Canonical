## April 22, 2026 — Provider palette, display names, tab coverage, status-panel dots
**Branch:** chore/provider-palette-and-display-names
**What:** Revised lane colors (Claude → warm orange #D97757, Claude Code → new yellow token #F5C518, Grok → burgundy #8B3A3A in both themes). Relocated PROVIDER_DISPLAY_NAMES from obsidian/renderer.py to importers/contracts.py and extended to all five providers. Registered provider_display_name Jinja filter. Wired filter into Workspace right panel and recent list. Added Claude Code and Grok tabs to /imported. Bumped right-panel provider dots from 8px to 10px.
**Decisions:**
- Claude Code moved to a distinct token (--lane-claude-code) rather than inheriting --lane-claude. The `.format-chip--claude-code` rule at app.css:1106 was the only rule sharing the token; rewired cleanly.
- Grok has no canonical brand color (xAI brand is monochrome by design), so the burgundy choice is purely a SoulPrint palette pick, not brand-matching.
- Kept PROVIDER_DESCRIPTIONS in obsidian/renderer.py; only DISPLAY_NAMES was relocated. Descriptions are still Obsidian-specific.
- Dot brighten kept to size bump only. Box-shadow / border / glow was considered and deferred as scope creep.
- Added .provider-tab--claude-code and .provider-tab--grok active-state CSS rules (did not exist previously).
**Next:** Phase C (row-action CSS promotion + star glyph unify + right-sidebar provider backlinks + left-rail cleanup). Then Phase A1 tags MVP per tagging-spec v2.
